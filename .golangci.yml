version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - goimports
    - misspell
    - unconvert
    - gocritic
  settings:
    goimports:
      local-prefixes: github.com/mikecafarella/permcop

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
